<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yandihard.tvmoviesapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TVMoviesApp">
        <activity android:name=".submission.activity.DetailFavoriteActivity"></activity>
        <activity android:name=".submission.activity.FavoriteActivity" />
        <activity android:name=".submission.activity.DetailTvshowActivity" />
        <activity android:name=".submission.activity.DetailMovieActivity" />
        <activity android:name=".submission.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>